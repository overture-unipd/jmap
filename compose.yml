version: '3.1'

services:
  server:
    image: overture-unipd/jmap:latest
    restart: unless-stopped
    ports:
      - 8000:8000

  storage:
    image: rethinkdb:2.4.2-bullseye-slim
    restart: unless-stopped
    volumes:
      - rethinkdb:/data

volumes:
  rethinkdb:
